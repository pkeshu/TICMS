<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item android:id="@+id/search"
        android:title="Search"
        android:icon="@drawable/ic_search_white_24dp"
        app:showAsAction="collapseActionView|ifRoom"
        app:actionViewClass="android.support.v7.widget.SearchView"
        android:visible="false"/>

    <item
        android:id="@+id/action_settings"
        android:title="Settings"
        app:showAsAction="never"
        android:visible="false"/>

    <item android:id="@+id/act_notification"
        android:title="Notification"
        android:icon="@drawable/ic_notification_icon"
        app:showAsAction="always"
        android:visible="false"
        app:actionLayout="@layout/costom_notification_layout"
        />

    <item android:id="@+id/profile"
        android:title="Profile"
        app:showAsAction="ifRoom"
        android:visible="false"
        app:actionLayout="@layout/user_profile"/>
    <item
        android:id="@+id/notiSend"
        android:title="Send"
        android:icon="@drawable/ic_send_black_24dp"
        app:actionLayout="@layout/custom_send_notification_button"
        android:visible="false"
        app:showAsAction="always"/>
</menu>
